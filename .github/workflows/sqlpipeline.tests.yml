name: SqlPipeline Pester Tests

on:
  push:
    branches:
      - dev
    paths:
      - 'SqlPipeline/**.ps1'
      - 'SqlPipeline/**.psm1'
    
  workflow_dispatch:

jobs:
  test:
    strategy:
      matrix:
        os: [ windows-latest ]
        shell: [ powershell ]


    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out repository code
        uses: actions/checkout@v5

      - name: Set up repositories - powershell
        if: matrix.shell == 'powershell'
        shell: powershell
        run: |
          Set-PSRepository psgallery -InstallationPolicy trusted
          Get-InstalledModule
          Install-Module WriteLog -Repository PSGallery -AllowClobber -Scope CurrentUser
          Install-Module ImportDependency -Repository PSGallery -Scope CurrentUser
          Install-Module SimplySQL -Force -Repository PSGallery -AllowClobber -Scope CurrentUser

      - name: Run Pester Tests (powershell on ${{ matrix.os }})
        if: matrix.shell == 'powershell'
        env:
          CI: true
        shell: powershell
        run: Invoke-Pester -Path SqlPipeline/Tests/ -Output Detailed -Passthru

      - name: Set up repositories - pwsh
        if: matrix.shell != 'powershell'
        shell: pwsh
        run: |
          Set-PSRepository psgallery -InstallationPolicy trusted
          Get-InstalledModule
          Install-Module WriteLog -Repository PSGallery -AllowClobber -Scope CurrentUser
          Install-Module ImportDependency -Repository PSGallery -Scope CurrentUser
          Install-Module SimplySQL -Force -Repository PSGallery -AllowClobber -Scope CurrentUser

      - name: Run Pester Tests (pwsh on ${{ matrix.os }})
        if: matrix.shell != 'powershell'
        env:
          CI: true
        shell: pwsh
        run: Invoke-Pester -Path SqlPipeline/Tests/ -Output Detailed -Passthru